# food-fit

A lightweight Flask app for meal planning and weight tracking experiments.

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Then open `http://localhost:5000`.

## Current features

- Daily weight tracker with an interactive line chart.
- Foundation for upcoming meal-planning calendar views.
